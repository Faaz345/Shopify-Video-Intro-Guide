<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Error</title>
  <style>
    body { font-family: system-ui, -apple-system, Segoe UI, Roboto, sans-serif; margin: 2rem; }
    .card { max-width: 620px; margin: 0 auto; padding: 1.5rem; border: 1px solid #fca5a5; border-radius: 8px; background: #fef2f2; }
    h2 { color: #991b1b; }
    .detail { color: #6b7280; font-size: .9rem; margin-top: .5rem; }
    a { color: #111827; }
  </style>
</head>
<body>
  <div class="card">
    <h2>Error</h2>
    <p><%= message %></p>
    <% if (detail) { %>
      <pre class="detail"><%= detail %></pre>
    <% } %>
    <p><a href="/">Go home</a></p>
  </div>
</body>
</html>

